import { TicketTearScreen } from "@/components/brand/ticket-tear";

/** Gate fallback. Deliberately terse copy — someone is holding a queue. */
export default function Loading() {
  return <TicketTearScreen label="Opening the gate" />;
}
